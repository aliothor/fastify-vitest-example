import { defineConfig } from 'vitest/config'

export default defineConfig({
    test: {
        // setupFiles: ['./test/server.setup.ts']，
        // poolOptions: {
        //     threads: {
        //         singleThread: true
        //     }
        // }
    }
})